export const metadata = {
  title: "Registers",
};

const RegistersLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <html lang="en" data-theme="cupcake">
      <body>{children}</body>
    </html>
  )
};

export default RegistersLayout;
